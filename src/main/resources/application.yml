server:
  port: 8090
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shelter_service
    username: root
    password: dud990531
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  application:
    name: shelter

naver:
  route:
    url: "https://maps.apigw.ntruss.com/map-direction/v1/driving?"
